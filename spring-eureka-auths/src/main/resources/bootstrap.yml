server:
  port: 8089
spring:
  application:
    name: auth-service
    
eureka:
  client:
    register-with-eureka: true
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}    