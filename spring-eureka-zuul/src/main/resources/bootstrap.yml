server:
  port: 9200
spring:
  application:
    name: zuul-server
    
eureka:
  client:
    register-with-eureka: true
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}    